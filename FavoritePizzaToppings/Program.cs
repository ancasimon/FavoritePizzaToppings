using System;
using System.Collections.Generic;
using System.Text.Json;
using System.Text.Json.Serialization;

namespace FavoritePizzaToppings
{
    class Program
    {
        static void Main(string[] args)
        {
            //var pizzas = new List<string>();
            //var pizzasJson = JsonSerializer.Serialize(pizzas, new JsonSerializerOptions { WriteIndented = true });

            //Pizza pizza1 = JsonSerializer.Deserialize<pizzas>
        }
    }
}
